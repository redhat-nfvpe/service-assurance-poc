apiVersion: v1
kind: Service
metadata:
  name: metrics
  labels:
    sa-app: metrics
spec:
  selector:
    sa-app: metrics
  type: ClusterIP
  clusterIP: None
  ports:
  - name: metrics
    port: 8081
    targetPort: 8081
    protocol: TCP
