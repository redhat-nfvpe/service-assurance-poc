apiVersion: v1
kind: Service
metadata:
  name: prometheus-operator
  labels:
    sa-app: prometheus-operator
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  selector:
    sa-app: prometheus-operator
